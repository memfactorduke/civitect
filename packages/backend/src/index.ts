/**
 * @civitect/backend — Supabase config, edge functions, sync client
 * (TDD §10 cloud sync, ADR-003/011).
 *
 * Placeholder shell: real wiring is deliberately last — ROADMAP Phase 10
 * syncs against a stable save format (ADR-010 migrations cover beta-era
 * saves). Until then this package only reserves the boundary.
 */
export const BACKEND_PACKAGE = "@civitect/backend";
