/**
 * ADR-013 gate — balance simulations (TDD §12.3).
 *
 * TODO(ROADMAP Phase 2): replace this no-op with the scenario runner:
 * parameterized playstyles ("zone R only", "no transit at 100k") asserted
 * against balance bands (e.g. unemployment within range by year 5), plots
 * exported to the balance dashboard.
 *
 * Stubbed because the demand model and cohort tables land in Phase 2 —
 * there is no economy to balance yet.
 */
console.log(
  "[gate:balance] STUB — no-op pass. Real gate lands with ROADMAP Phase 2 (ADR-013 §3, TDD §12.3).",
);
